package za.ac.ngosa.factory;

/**
 * Created by OWNER on 2015/04/24.
 */
/*
public class FactoryTests {


    @Test
    public void testReceipt() throws Exception {

        Map<String, String> values = new HashMap<String, String>();
        List<Movie> movies= new ArrayList<Movie>();


        values.put("cashierName", "Jerry");
        values.put("item","large popcorn");

        Receipt receipt= ReceiptFactory.createReceipt(values, 60.00, 5, movies, 444);
        junit.framework.Assert.assertEquals(444, receipt.getId());
        Assert.assertNotNull(receipt);
    }

    @Test
    public void testBeverage() throws  Exception{

        Map<String,String> values= new HashMap<String,String>();

        // values.put("code","")
        values.put("name","cream soda");
        values.put("category","soda");

        Beverage beverage= ConsumableFactory.createBeverage(values, Long.valueOf(556), 10.00, 330.50);

        junit.framework.Assert.assertEquals(556, beverage.getCode());
        Assert.assertNotNull(beverage);
    }

    @Test
    public void testFood() throws  Exception{

        Map<String,String> values= new HashMap<String,String>();


        values.put("name","whispers");
        values.put("category","candy");

        Food food= ConsumableFactory.createFood(values,556, 15.00, 250.00);

        junit.framework.Assert.assertEquals(556, food.getCode());
        Assert.assertNotNull(food);
    }

    @Test
    public void testManager() throws Exception {

        Map<String, String> values = new HashMap<String, String>();


        values.put("jobTitle", "F&B Manager");
        values.put("phoneNumber", "09448754");
        values.put("name", "jack");
        values.put("department", "F&B");


        Employee manager = EmployeeFactory.createManager(values, 556, 674);

        junit.framework.Assert.assertEquals("F&B Manager", manager.getJobTitle());
        Assert.assertNotNull(manager);
    }

    @Test
    public void testMovie() throws Exception {

        Map<String, String> values = new HashMap<String, String>();


        values.put("type", "3D");
        values.put("genre", "drama");
        values.put("duration", "1hr 56min");
        values.put("title", "The perks of being a wallflower");
        Movie movie= ShowingFactory.createMovie(values, 60.00, 556);
        junit.framework.Assert.assertEquals("drama", movie.getGenre());
        Assert.assertNotNull(movie);
    }

    @Test
    public void testSchedule() throws Exception {

        Map<String, String> values = new HashMap<String, String>();


        values.put("title", "mad max");

        Schedule schedule = ScheduleFactory.createSchedule(values, 2, 444, 12);
        junit.framework.Assert.assertEquals("mad max", schedule.getTitle());
    }

    @Test
    public void testScreeningRoom() throws Exception {

        Map<String, String> values = new HashMap<String, String>();


        values.put("type", "IMAX 3D");

        ScreeningRoom screeningRoom = ScreeningRoomFactory.createScreeningRoom(values, 250, 674);
        junit.framework.Assert.assertEquals("IMAX 3D", screeningRoom.getType());
    }

    @Test
    public void testTVShow() throws Exception {

        Map<String, String> values = new HashMap<String, String>();


        values.put("season", "2");
        values.put("genre", "fantasy");
        values.put("duration", "1hr");
        values.put("title", "Game of thrones");
        TVShow tvShow= ShowingFactory.createTVShow(values, 674, 60.00);
        junit.framework.Assert.assertEquals("fantasy", tvShow.getGenre());
    }
}*/
